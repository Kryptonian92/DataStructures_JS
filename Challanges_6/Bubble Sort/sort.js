// Sorting using builting methods

function compareByLen(str1, str2){
    console.log(str2.length - str2.length)
    return str2.length - str2.length;
}
[ "Lamborghini", "Ausar", "Data Structures", "Algorithms"]
    .sort(compareByLen);

// sorts arrays